# results/ 目錄說明

此目錄用於存放最終回測結果。

## 用途

- `divergenceplus.py` 的完整回測輸出（勝率、期望值、年份分拆表）
- 標準化的績效報告（含交易明細、Sharpe、MDD 等）

## 尚未填充

首次回測完成後，請執行：

```bash
python scripts/divergenceplus.py
```

結果將輸出到 `output/` 目錄，整理後移至此處存檔。
